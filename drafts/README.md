# drafts/

Finished article drafts land here as `[slug].md` after the pipeline:

`researcher` → `head-of-research` → `writer` → `humanizer` → `linker` → `editor-in-chief`

Each draft includes YAML frontmatter (meta title, description, word count, stage).

After human approval, integrate copy into `src/i18n.js` and `index.html` per `CLAUDE.md`.

Files in this folder are gitignored (except this README).
